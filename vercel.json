{
	"version": 2,
	"buildCommand": "pnpm install:all && pnpm build:all",
	"devCommand": "pnpm dev",
	"outputDirectory": "server",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/server/src/index.js"
		}
	]
}
